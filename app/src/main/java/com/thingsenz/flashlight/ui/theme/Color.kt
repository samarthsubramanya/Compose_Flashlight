package com.thingsenz.flashlight.ui.theme

import androidx.compose.ui.graphics.Color

// Beam / "on" state — warm halogen glow
val BeamAmber = Color(0xFFFFC96B)
val BeamAmberBright = Color(0xFFFFE1A3)

// Torch body
val BodySteel = Color(0xFF3A3B40)
val BodySteelDark = Color(0xFF232428)

// Base surfaces — near-black, purpose-built (not tied to system wallpaper)
val VoidBlack = Color(0xFF08080A)
val PanelBlack = Color(0xFF141417)
val PanelBlackElevated = Color(0xFF1C1C20)
val OutlineDim = Color(0xFF2C2C31)

// Text
val TextPrimary = Color(0xFFF3F3F0)
val TextMuted = Color(0xFFA5A5AC)

// Mode accents
val SosIdle = Color(0xFFFF5C5C)
val SosActive = Color(0xFF54D68A)
val StrobeIdle = Color(0xFF8A8A93)
val StrobeActive = Color(0xFF7FB8FF)
